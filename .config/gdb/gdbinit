set debuginfod enable off
define cls
    shell clear
end

define pm
  set $i = 0
  while ($i < $arg0.size())
    print $arg0[$i]
    set $i = $i + 1
  end
end

set history save
set verbose off
set print pretty on
set print array off
set print array-indexes on
set auto-load safe-path /
